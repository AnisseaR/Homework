# Section 7.1

### Question 1

SELECT ModelYear, Make, Model 
FROM EVRegistry 

### Question 2

SELECT DISTINCT(ElectricVehicleType)
FROM EVRegistry 

### Question 3 

SELECT *
FROM EVRegistry 
WHERE ElectricVehicleType = "Battery Electric Vehicle (BEV)"

### Question 4

SELECT Make, Model
FROM EVRegistry 
WHERE BaseMSRP BETWEEN 20000 AND 35000

# Section 7.2

### Question 1

SELECT *
FROM EVRegistry 
WHERE City IS NULL

### Question 2 

SELECT Make, Model, ElectricVehicleType
FROM EVRegistry 
WHERE VIN LIKE '%3E1EA1J'

### Question 3 

SELECT ModelYear, Make, Model, ElectricVehicleType, ElectricRange
FROM EVRegistry 
WHERE Make IN ('TESLA','CHEVROLET')
ORDER BY Make, ModelYear DESC

### Question 4 

SELECT stationID, COUNT(sessionid) AS TimesUsed
FROM EVCharging
GROUP BY stationId
ORDER BY TimesUsed DESC
LIMIT 5

### Question 5 

SELECT userId,  MIN(chargeTimeHRS) AS minTime, MAX(chargeTimeHrs)AS maxTime
FROM EVCharging 
WHERE chargeTimeHrs > 0.5
GROUP BY userId
ORDER BY minTime DESC, maxTime ASC

# Section 7.3

### Question 1

SELECT weekday, ROUND(AVG(chargeTimeHrs),2) AS maxTimeAvg
FROM EVCharging 
GROUP BY weekday
ORDER BY maxTimeAvg DESC
LIMIT 1

### Question 2

SELECT userId, ROUND(SUM(kwhTotal),2) AS totalPower
FROM EVCharging 
GROUP BY userId
ORDER BY totalPower DESC
LIMIT 15

### Question 3 

SELECT dimFacility.typeFacility, COUNT(factCharge.facilityID) AS numStation
FROM factCharge LEFT JOIN dimFacility ON factCharge.facilityID = dimFacility.FacilityKey
GROUP BY dimFacility.typeFacility
ORDER BY numStation DESC
LIMIT 15

## Question 4

A primary key identifies a unique value in a table and a foreign key refers back to the primary key.

## Question 5

SELECT userId, MIN(chargeTimeHrs) AS minTime, MAX(chargeTimeHrs) AS maxTime
FROM EVCharging
WHERE chargeTimeHrs > 1
GROUP BY userId
HAVING MIN(chargeTimeHrs) > 1
ORDER BY minTime, maxTime

